On utilisé la commande git rebase quand on était dans la branche fork2, puis on est retourné dans la branche master pour faire un git merge. 
La différence entre Rebase et Merge est que la brache fork2 pointe vers la branche master donc à la fin on ne voit qu'une branche au lieu de 2.

